import { shallowMount } from "@vue/test-utils";
import cmp from "../lib/grid";

describe("grid", () => {
	// test("renders props", () => {
	// 	const msg = "111";
	// 	const wrapper = shallowMount(cmp, {
	// 		propsData:{msg}
	// 	});
	// 	expect(wrapper.text()).toMatch(msg);
	// });
});
